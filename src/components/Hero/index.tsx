import { HeroContainer, HeroTitle } from './Hero'

const Hero = () => (
  <HeroContainer>
    <div className="container">
      <HeroTitle>
        As melhores vagas para tecnologia, design e artes visuais.
      </HeroTitle>
    </div>
  </HeroContainer>
)

export default Hero
